/*Ques01: EmployeeCRUD (Spring Core + JDBC)
march_25_2026_test_01_EmployeeCRUD
Objective:
Create a Layered App (Controller → Service → DAO) to perform CRUD on Employee.

Table:
employee(id, name, department, salary)

Layers:

Controller → user input/output
Service → logic
DAO → JDBC (DriverManager, PreparedStatement)

Operations:

Add
Delete
Get All

Config:
Use @Configuration + @Bean

Rule:
Spring Core only + Plain JDBC + proper layering*/
